FactoryBot.define do
  factory :product_type do
    name { "Bicycle" }
    description { "A bicycle, also called a pedal cycle, bike, push-bike or cycle, is a human-powered or motor-assisted, pedal-driven, single-track vehicle, with two wheels attached to a frame, one behind the other." }
  end
end
